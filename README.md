# 🎵 Music Management System API

Это RESTful API на базе **Spring Boot**, разработанное в рамках итогового проекта (Endterm). Приложение позволяет управлять музыкальным контентом, используя современные практики разработки на Java.

---

## 🚀 Основные возможности (Core Features)
* **RESTful Endpoints:** Полный CRUD для управления музыкой.
* **Database Integration:** Использование встроенной БД **H2** (In-memory).
* **OOP Architecture:** Реализация наследования через базовый абстрактный класс `MusicContent`.
* **JPA Inheritance:** Применение стратегии `SINGLE_TABLE` для эффективного хранения иерархии объектов в одной таблице.

---

## 🛠 Технологический стек
* **Java 25** (OpenJDK)
* **Spring Boot 3.2.2**
* **Spring Data JPA** (для работы с БД)
* **Maven** (сборка и зависимости)
* **H2 Database** (быстрая БД в оперативной памяти)

---

## 📂 Структура проекта
Проект следует архитектуре разделения ответственности:
* `model` — Сущности (Entities), описывающие структуру данных.
* `repository` — Интерфейсы для взаимодействия с базой данных.
* `service` — Бизнес-логика приложения.
* `controller` — Обработка HTTP-запросов и REST-эндпоинты.



---

## ⚙️ Настройка и запуск
1. **Клонирование:** `git clone <url>`
2. **Сборка проекта:**
   ```bash
   mvn clean install
